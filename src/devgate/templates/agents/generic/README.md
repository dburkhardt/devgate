# devgate Agent Adapter

Canonical instructions live at:

`~/.agents/devgate/SKILL.md`

Use devgate helpers for forwarded ports and artifact publishing.

The remote machine is the source of truth. A local mirror such as
`~/Remote/<host>` may exist for inspection only; do not write there to affect
remote files.
